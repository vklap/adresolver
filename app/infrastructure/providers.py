def store_connection_wrapper(session_id, connection_wrapper):
    pass


def get_connection_wrapper_by_session(session_id):
    pass


def send_session_to_queue(session_id):
    # TODO: send session_id to queue, to its data can be handled later by a background job/task
    pass